<div class="row">
  <div class="col-xs-12 col-md-6">
    <h2>Listing Drivers</h2>
  </div>
</div>
<div class="row">
  <div class="col-xs-12 col-md-6">
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Last name</th>
          <th>First name</th>
          <th>Phone number</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @drivers.each do |driver| %>
          <tr>
            <td><%= driver.last_name %></td>
            <td><%= driver.first_name %></td>
            <td><%= driver.phone_number %></td>
            <td><%= link_to 'Edit', edit_driver_path(driver) %></td>
            <td><%= link_to 'Destroy', driver, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<br>
<div class="row">
  <div class="col-xs-12 col-md-6">
<%= link_to 'New Driver', new_driver_path, class: 'btn btn-primary' %> | <%= link_to 'New Availability', new_availability_path, class: 'btn btn-primary' %>
  </div>
</div>